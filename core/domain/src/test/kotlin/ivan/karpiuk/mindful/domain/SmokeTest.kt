package ivan.karpiuk.mindful.domain

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Trivial smoke test that proves JUnit 4 is correctly configured
 * for the :core:domain module. Delete once real domain tests exist.
 */
class SmokeTest {
    @Test
    fun `junit 4 is correctly configured`() {
        assertEquals("JUnit 4 works", "JUnit 4 works")
    }
}
